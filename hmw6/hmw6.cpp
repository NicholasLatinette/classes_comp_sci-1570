#include "hmw6.h"
int main ()
{
  int choice;
  float min_val, max_val;
  srand (time(NULL));
  choice = displaymenu();
  quiz(choice, min_val, max_val)
  function running
  right - wrong
  right %100 = percentage
  return right



      run until kwit
      return 0;
    }
}
